почистить все каллоки на null СДЕЛАНО
проверить перед атофов(atoi), что это число - взять проверку из калькулятора СДЕЛАНО

в запуске в дебагере в силионе в run -> edit configuration -> working directory -> src нужно щелкнуть на папочку и выбрать папку src, чтобы можно было бы найти файл из папки

подумать чтобы в гет инт проверить ошибки

в таблице координат вершин - первая строчка не будет использоваться, так как для прорисовки отсчет вершин начинается с 1. В таблице прорисовок - все, как всегда

история без вершин и/или полигонов будет восприниматься как ошибочная

подумать, будет ли ошибкой, если в полигоне соединяются одна и та же вершина ПОКА ОСТАВЛЮ
подумать, будет ли ошибкой, если в полигоне соединяются вершина, номера которой нет СДЕЛАНО
подумать, будет ли ошибкой, если в полигоне 0 вершина или отрицательная - отрицательные уже зашиты СДЕЛАНО

подумать, будет ли ошибкой, если в полигоне больше 2х// 1/1/1/1 ? пока оставлю

потом удалить лишние null после free СДЕЛАНО

нужно узнать, может ли w быть отрицательным числом?

сделать очередь для rotate будет зашито на фронте

rotate будет в радианах ОСТАВЛЯЕМ

в случае плохих исходов аффиных преобразований это должно не ломать программу, а просто не будет происходить перезаписи матрицы

если не добавится дополнительных кейсов, убрать проверку столбцов и строк в умножении матриц

в масштабировании если не изменяется координата, то передавать надо 1, а не 0

в тесте с множественными кручениями нет цифр проверок, лишь общий статус (s21_rotate_XYZ)

перед сдачей удалить из кода принты все

потом отработать ненужные возвращаемые значения в виде интов (типа для move, center...)

проверить красоту тестов на is_ok

подумать, чтобы preparation_to_init_draw разделить внутри на две функции СДЕЛАНО

засунуть рекалькулейт экстремумов во все аффиные преобразования ПРЕДВАРИТЕЛЬНО СДЕЛАНО

сейчас у меня поле [-1;1]

подумать, так ли нам нужна в coord_matrix struct cols если она всегда 3